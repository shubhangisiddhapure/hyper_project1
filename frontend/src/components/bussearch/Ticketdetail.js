import React, { useState } from "react";
const Ticketdetail = props => {
  const ticketInfo = props.location.state.detail;
  console.log(ticketInfo);
  return <div>shree</div>;
};

export default Ticketdetail;
